#edit  ~ @RR9R7 for Jmthon 

from telethon import events
from userbot.utils import admin_cmd
from userbot import jmthon
from . import *

plugin_category = "extra"
@jmthon.ar_cmd(
    pattern="ح1$",
    command=("ح1", plugin_category),
           )
async def tmgif(event):
    if event.fwd_from:
        return
    RR9R7 = await reply_id(event)
    if fun:
        jmthon = f"**{RR9R7}**\n"
        jmthon += f"✛━━━━━━━━━━━━━✛\n"
        jmthon += f"**الـمتحـرڪـة الأولـى **\n"
        jmthon += f"✛━━━━━━━━━━━━━✛"
        await event.client.send_file(event.chat_id, fun, caption=jmthon, reply_to=RR9R7)

#edit  ~ @RR9R7 for Jmthon 
@jmthon.ar_cmd(
    pattern="ح2$",
    command=("ح2", plugin_category),
           )
async def tmgif(event):
    if event.fwd_from:
        return
    leo = await reply_id(event)
    if fun2:
        RR9R7 = f"**{RR9R7}**\n"
        RR9R7 += f"✛━━━━━━━━━━━━━✛\n"
        RR9R7 += f"**الـمتحـرڪـة الـثـانيـة **\n"
        RR9R7 += f"✛━━━━━━━━━━━━━✛"
        await event.client.send_file(event.chat_id, fun2, caption=RR9R7, reply_to=leo)
#edit  ~ @RR9R7 for Jmthon 

@jmthon.ar_cmd(
    pattern="ح3$",
    command=("ح3", plugin_category),
           )
async def tmgif(event):
    if event.fwd_from:
        return
    sic_id = await reply_id(i)
    if fun3:
        RAZAN = f"**{RR9R7}**\n"
        RAZAN += f"✛━━━━━━━━━━━━━✛\n"
        RAZAN += f"**الـمتحـرڪـة الـثـالثـة**\n"
        RAZAN += f"✛━━━━━━━━━━━━━✛"
        await event.client.send_file(event.chat_id, fun3, caption=RAZAN, reply_to=sic_id)
#edit  ~ @RR9R7 for Jmthon 

@jmthon.ar_cmd(
    pattern="ح4$",
    command=("ح4", plugin_category),
           )
async def tmgif(event):
    if event.fwd_from:
        return
    reply_to_id = await reply_id(event)
    if fun4:
        RAZAN = f"**{RR9R7}**\n"
        RAZAN += f"✛━━━━━━━━━━━━━✛\n"
        RAZAN += f"**الـمتحـرڪـة الـرابـعـة**\n"
        RAZAN += f"✛━━━━━━━━━━━━━✛"
        await event.client.send_file(
            event.chat_id, fun4, caption=RAZAN, reply_to=reply_to_id
        )

#edit  ~ @RR9R7 for Jmthon 
@jmthon.ar_cmd(
    pattern="ح5$",
    command=("ح5", plugin_category),
           )

async def tmgif(event):
    if event.fwd_from:
        return
    reply_to_id = await reply_id(event)
    if fun5:
        RAZAN = f"**{RR9R7}**\n"
        RAZAN += f"✛━━━━━━━━━━━━━✛\n"
        RAZAN += f"**الـمتحـرڪـة الـخامسـة**\n"
        RAZAN += f"✛━━━━━━━━━━━━━✛"
        await event.client.send_file(
            event.chat_id, fun5, caption=RAZAN, reply_to=reply_to_id
        )

#edit  ~ @RR9R7 for Jmthon 

@jmthon.ar_cmd(
    pattern="ح6$",
    command=("ح6", plugin_category),
           )

async def tmgif(event):
    if event.fwd_from:
        return
    reply_to_id = await reply_id(event)
    if fun6:
        RAZAN = f"**{RR9R7}**\n"
        RAZAN += f"✛━━━━━━━━━━━━━✛\n"
        RAZAN += f"**الـمتحـرڪـة الـسادسـة**\n"
        RAZAN += f"✛━━━━━━━━━━━━━✛"
        await event.client.send_file(
            event.chat_id, fun6, caption=RAZAN, reply_to=reply_to_id
        )

#edit  ~ @RR9R7 for Jmthon 

@jmthon.ar_cmd(
    pattern="ح7$",
    command=("ح7", plugin_category),
           )
async def tmgif(event):
    if event.fwd_from:
        return
    reply_to_id = await reply_id(event)
    if fun7:
        RAZAN = f"**{RR9R7}**\n"
        RAZAN += f"✛━━━━━━━━━━━━━✛\n"
        RAZAN += f"**الـمتحـرڪـة الـسـابعـة**\n"
        RAZAN += f"✛━━━━━━━━━━━━━✛"
        await event.client.send_file(
            event.chat_id, fun7, caption=RAZAN, reply_to=reply_to_id
        )

  
#edit  ~ @RR9R7 for Jmthon 
#جميع الحقوق محفوظة لسـورس جـمثون تخـمط تبيـن فشلـك